import { content } from './loadContent'

/** Everything content/courses.yaml publishes, read-only for the whole app. */
export const data = content.data
export const site = content.site
export const missingFiles = content.missingFiles
export const draftCount = content.draftCount
